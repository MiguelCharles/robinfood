<h1>Orders#index</h1>
<p>Find me in app/views/orders/index.html.erb</p>


<div class="row">
   <% current_user.orders.each do |order| %>
      <div class ="col-md-4">
        <div class="card text-center">
            <h2> <%= link_to order.promotion.title, order_path(order)%></h2>
        </div>
        </div>
          <%end%>
        </div>
